// replaces default partial rendering code with this, which is the same but has the updated
// show id from the controller action, which then gets passed to the partial 'show_info_modal'
$(".show-info").html("<%= escape_javascript(render partial: 'show_info_modal', locals: { show_id: @showID } ) %>");
